{% extends "base.html.twig" %}
{# ICI ON MET LE BODY DE LA PAGE HOME #}
{% block body %}
<!-- CAROUSEL -->
<div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
       <img src="{{asset('img/home-img-1.jpg')}}"/>
    </div>
    <div class="carousel-item">
      <img src="{{asset('img/home-img-4.jpg')}}"/>
    </div>
  </div>
</div>
<!-- CAROUSEL -->
<!-- RECIPES -->
<div class="container">

<!-- RCP 1 -->
{% for recipe in recipe %}
<div class="col-sm">
<div class="card card__style" style="width: 18rem;">
  <img class="img__style"src="{{ recipe.image }}">
  <div class="card-body">
    <h5 class="card-title">{{ recipe.title }}</h5>
    <p class="card-text">{{ recipe.description }}</p>
    <a href="/recettes/{{recipe.id}}" class="btn btn-primary btn__style">Voir la recette</a>
  </div>
</div>
{% endfor %}

</div>
{% endblock %}